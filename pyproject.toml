[project]
name = "boardcomposer"
version = "0.1.0"
description = "BoardComposer"
requires-python = ">=3.13,<3.14"

dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=8.4",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
